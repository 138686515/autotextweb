name: 自动化接口测试

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

# 添加权限配置，解决GitHub Pages部署403错误
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 检出代码
        uses: actions/checkout@v4

      - name: 设置 Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 安装 Allure
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jre-headless
          wget https://github.com/allure-framework/allure2/releases/download/2.35.1/allure-2.35.1.tgz
          tar -xzf allure-2.35.1.tgz
          sudo mv allure-2.35.1 /opt/allure
          sudo ln -s /opt/allure/bin/allure /usr/bin/allure

      - name: 运行测试
        run: python -m pytest --alluredir=reports/allure

      - name: 生成 Allure 报告
        run: allure generate reports/allure -o reports/allure-report --clean

      - name: 上传 Allure 报告
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: reports/allure-report
          retention-days: 7

      - name: 安装 Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 安装 Netlify CLI
        run: npm install -g netlify-cli

      - name: 部署到Netlify
        run: |
          # 设置NETLIFY_SITE_ID和NETLIFY_AUTH_TOKEN环境变量
          netlify deploy --dir=reports/allure-report --prod --site=$NETLIFY_SITE_ID --auth=$NETLIFY_AUTH_TOKEN
        env:
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}

      - name: 部署报告到GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./reports/allure-report
          publish_branch: gh-pages
          force_orphan: true
        continue-on-error: true

      - name: 发送钉钉通知
        if: always()  # 无论测试结果如何，都发送通知
        run: |
          pip install requests
          cat > send_dingtalk.py << 'EOF'
          import os
          import requests
          import json
          from datetime import datetime

          # 获取GitHub环境变量
          repo = os.environ['GITHUB_REPOSITORY']
          run_id = os.environ['GITHUB_RUN_ID']
          workflow = os.environ['GITHUB_WORKFLOW']
          run_url = f"https://github.com/{repo}/actions/runs/{run_id}"
          job_status = "${{ job.status }}"

          # 获取钉钉Webhook
          webhook_url = os.environ['DINGTALK_WEBHOOK']

          # 解析Allure结果数据，获取测试用例统计信息
          def get_test_statistics():
              allure_results_dir = "reports/allure"
              stats = {
                  "total": 0,
                  "passed": 0,
                  "failed": 0,
                  "broken": 0,
                  "skipped": 0
              }
              
              try:
                  import os
                  for filename in os.listdir(allure_results_dir):
                      if filename.endswith('.json'):
                          file_path = os.path.join(allure_results_dir, filename)
                          with open(file_path, 'r', encoding='utf-8') as f:
                              data = json.load(f)
                          if isinstance(data, dict) and data.get('uuid') and data.get('status'):
                              stats["total"] += 1
                              status = data["status"]
                              if status == "passed":
                                  stats["passed"] += 1
                              elif status == "failed":
                                  stats["failed"] += 1
                              elif status == "broken":
                                  stats["broken"] += 1
                              elif status == "skipped":
                                  stats["skipped"] += 1
              except Exception as e:
                  print(f"获取测试统计信息失败: {str(e)}")
              
              return stats

          # 获取测试用例统计信息
          stats = get_test_statistics()

          # 构建报告URL
          username = repo.split('/')[0]
          repo_name = repo.split('/')[1]
          
          # GitHub Pages报告URL
          github_pages_url = f"https://{username}.github.io/{repo_name}/"
          
          # Netlify报告URL（请替换为你的实际Netlify站点URL）
          # 格式：https://你的站点名称.netlify.app
          netlify_url = "https://heroic-banoffee-9aac3c.netlify.app"
          
          # 基于测试用例结果确定状态，而不是工作流状态
          if stats["failed"] > 0 or stats["broken"] > 0:
              status = "failure"
              status_emoji = "❌"
          else:
              status = "success"
              status_emoji = "✅"
          
          # 构建通知内容
          message = f"""
          【自动化接口测试报告】
          {status_emoji} 测试状态: {status}
          📅 测试时间: {datetime.now().strftime("%Y-%m-%d %H:%M:%S")}
          📋 工作流名称: {workflow}
          🔗 运行详情: {run_url}
          📊 测试结果统计:
            - 总用例数: {stats['total']}
            - 通过: {stats['passed']}
            - 失败: {stats['failed']}
            - 错误: {stats['broken']}
            - 跳过: {stats['skipped']}
          🌐 在线报告:
            - GitHub Pages: {github_pages_url}
            - Netlify: {netlify_url}
          📥 报告下载: https://github.com/{repo}/actions/runs/{run_id}/artifacts
          """

          # 发送钉钉消息
          payload = {
              "msgtype": "text",
              "text": {
                  "content": f"自动测试 {message}"
              }
          }

          response = requests.post(webhook_url, json=payload)
          response.raise_for_status()
          print("钉钉通知发送成功")
          EOF

          python send_dingtalk.py
        env:
          DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}